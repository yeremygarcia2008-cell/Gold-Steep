name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://parra-es-hermoso.onrender.com/
          echo ""
          curl -s -o /dev/null -w "%{http_code}" https://parra-es-hermoso.onrender.com/api/settings
          echo ""
